<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <title>Zapomniałem hasła</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #333;
        }
        .container {
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
            box-sizing: border-box;
            text-align: center;
        }
        .container h1 {
            text-align: center;
            color: #333;
            margin-bottom: 20px;
        }
        .container label {
            display: block;
            margin-bottom: 10px;
            color: #555;
            font-weight: bold;
        }
        .container input {
            width: 100%;
            padding: 12px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
            font-size: 16px;
        }
        .container button {
            width: 100%;
            padding: 12px;
            background-color: #2887a7;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s ease;
            margin-top: 10px;
        }
        .container button:hover {
            background-color: #1c6f8b;
        }
        .container .back-link {
            display: block;
            text-align: center;
            margin-top: 10px;
            color: #2887a7;
            text-decoration: none;
            font-weight: bold;
        }
        .container .back-link:hover {
            text-decoration: underline;
        }
        .container p.error {
            color: red;
        }
        .container p.message {
            color: green;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Resetowanie hasła</h1>
        <form action="/request-password-reset" method="post">
            <label for="email">Adres email:</label>
            <input type="email" id="email" name="email" required>
            <button type="submit">Wyślij kod resetowania hasła</button>
        </form>
        <% if (error) { %>
            <p class="error"><%= error %></p>
        <% } %>
        <% if (message) { %>
            <p class="message"><%= message %></p>
        <% } %>
        <a class="back-link" href="/login">Powrót do logowania</a>
    </div>
</body>
</html>
